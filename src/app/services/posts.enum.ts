export enum PostsSorting {
    byPictureDate = 'pictureDate',
    byAuthor = 'author',
    byDate = 'date',
}

export enum RepliesSorting {
    isRead = 'is_read',
    byAuthor = 'author',
    byDate = 'date',
    byCommentId = 'comment_id'
}
